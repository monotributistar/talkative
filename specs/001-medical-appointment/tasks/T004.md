# T004 — Ejecución, consentimiento y evidencia persistentes

**Owner:** Domain  
**Estado:** done  
**Depende de:** T002, T003  
**Archivos permitidos:** `backend/prisma/schema.prisma`, `backend/src/cases/**`,
`specs/001-medical-appointment/tasks/T004.md`

## Objetivo

Extender el expediente de T003 con `Task`, `Attempt`, `Consent`, `Artifact` y
`CalendarProjection`, incluyendo constraints de idempotencia, tenant isolation
y repositorios tenant-safe.

## Contratos

- Consume: modelos T003, `TaskCommand`, `AdapterResult` y
  `MedicalAppointmentCompletion`.
- Produce: persistencia para ejecución, autorización y evidencia.
- No puede cambiar: contratos T002, rutas, adaptadores ni lógica conversacional.

## Escenarios obligatorios

```gherkin
Scenario: idempotencia de intentos
  Given una tarea y una idempotency key existentes
  When se intenta crear el mismo efecto otra vez
  Then se recupera o rechaza el duplicado sin crear un segundo Attempt

Scenario: consentimiento revocado
  Given un consentimiento revocado
  When se consulta como autorización vigente
  Then no se lo considera válido

Scenario: proyección de calendario única
  Given un caso con una CalendarProjection
  When se intenta registrar otra proyección para el mismo proveedor y evento
  Then el constraint impide el duplicado
```

## Definition of Done

- [x] Relaciones e índices Prisma.
- [x] Claves únicas para idempotencia y calendario.
- [x] Repositorios con `tenant_id` obligatorio.
- [x] Reloj e IDs inyectables.
- [x] Tests feliz, límite y fallo.
- [x] Prisma client generado.
- [x] Test focal y typecheck backend.

## Comandos de validación

```bash
pnpm --filter workflow-agent-backend prisma:generate
pnpm --filter workflow-agent-backend test
pnpm --filter workflow-agent-backend typecheck
```

## Evidencia de entrega

- Modelos Prisma: `Task`, `Attempt`, `Consent`, `Artifact` y
  `CalendarProjection`, relacionados al expediente por claves tenant-safe.
- Idempotencia: unique por `(tenantId, idempotencyKey)` en intentos; reintentos
  con la misma clave recuperan el registro y una reutilización incompatible se
  rechaza.
- Calendario: uniques por evento, idempotency key y caso/proveedor; el
  repositorio recupera una repetición exacta y rechaza conflictos.
- Consentimiento: consulta vigente considera estado, revocación, vigencia,
  contraparte, scope y categorías mínimas.
- `prisma validate`: schema válido.
- `prisma migrate diff --from-empty --to-schema-datamodel`: SQL generado con
  las cinco tablas y sus índices únicos, sin conexión ni datos reales.
- Test focal acumulado Domain: 10/10.
- Suite backend: 102/102.
- Typecheck backend: correcto.
- Lint focal de `backend/src/cases`: correcto.

## Revisión del integrador

- Se endureció la idempotencia de `Attempt`: la misma clave sólo recupera el
  registro cuando task, sequence, channel y correlation ID coinciden.
- Se endureció la idempotencia de calendario: la misma identidad sólo recupera
  la proyección cuando también coinciden evento externo y horarios.
- Se agregaron regresiones para ambos conflictos.
- Gates integrados: Domain 10/10, backend 102/102, contratos 11/11, typecheck,
  lint, build, Prisma generate, Prisma validate y migration diff correctos.
